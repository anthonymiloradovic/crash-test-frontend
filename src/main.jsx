import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
import './styles/index.scss';

// Remplacez ReactDOM.render() par ReactDOM.createRoot().render()
ReactDOM.createRoot(document.getElementById('root')).render(<App />);
